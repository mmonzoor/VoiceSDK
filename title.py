import sys
print(sys.version)

names = pd.read_csv('microbial_names.csv')
print(names)

# dependency parser for taking out what they think the  
# looking at entropy and making sure that only half shows up. 